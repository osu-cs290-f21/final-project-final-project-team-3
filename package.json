{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "handlebars views/partials/leaderboardItem.handlebars -f public/leaderboardItemTemplate.js",
    "prestart": "npm run build",
    "predev": "npm run build",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "express": "^4.17.1",
    "express-handlebars": "^6.0.2",
    "handlebars": "^4.7.7",
    "nodemon": "^2.0.15"
  }
}
